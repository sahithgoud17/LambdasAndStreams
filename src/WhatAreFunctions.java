interface Movie{
    boolean isClassic(int movieId);
}
interface Person{
    boolean isEmployee(int empId);
}
interface Hospital{
    boolean admit(int patientId);
}
interface Predicate<T>
{
    boolean test(T t);
}
public class WhatAreFunctions {

}